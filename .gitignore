node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
# .env holds only the local SQLite path (no secrets) and is committed so the app
# runs on a fresh clone. Real secrets/overrides go in .env*.local (ignored).
.env*.local
.env.local
prisma/dev.db
prisma/dev.db-journal
next-env.d.ts
.vercel
*.tsbuildinfo
